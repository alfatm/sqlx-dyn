error: another interpolation appears inside the same predicate as `${?...}`.
       The predicate must be removable as one piece, while each interpolation is expanded separately, so the two cannot overlap: `a = ${?x} || ${y}` would drop to `... FROM t $1`.
       Give the optional condition its own `AND` clause, or use a plain bind `${...}`.
  --> tests/compile_fail/opt_marker_in_predicate_tail.rs:10:12
   |
10 |     query!("SELECT * FROM t WHERE a = ${?x} || ${y}");
   |            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
