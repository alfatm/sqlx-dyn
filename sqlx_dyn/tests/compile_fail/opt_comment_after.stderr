error: SQL comments are not supported in a template using `${?..}`.
       A comment after the marker hides the keyword that follows it: the predicate tail stops at that keyword, and the newline closing a line comment is dropped, so the surviving keyword ends up commented out and the query silently matches the wrong rows.
       Remove the comment or build this query without `${?..}`.
 --> tests/compile_fail/opt_comment_after.rs:9:12
  |
9 |     query!("SELECT * FROM t WHERE a = ${?x} -- note\nAND b = 1");
  |            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
